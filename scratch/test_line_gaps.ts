// Test if Lightweight Charts LineSeries connects across whitespace points or if there's a way to break it
import { createChart } from "lightweight-charts";
// Since lightweight-charts runs in browser, we can just check types or see if we can find documentation about it.
console.log("Lightweight-charts imported successfully.");
